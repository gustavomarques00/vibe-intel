{
  "name": "vibe-intel",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev:api": "pnpm --filter @vibe/api dev",
    "dev:ui": "pnpm --filter @vibe/ui dev",
    "dev:core": "pnpm --filter @vibe/core dev"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
