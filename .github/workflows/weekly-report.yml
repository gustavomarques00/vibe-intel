name: 📊 Relatório Semanal

on:
  schedule:
    - cron: "0 9 * * 1" # Toda segunda às 9h
  workflow_dispatch:

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      # 1️⃣ Instalar PNPM global ANTES de qualquer uso
      - name: 🧩 Instalar PNPM globalmente
        run: |
          npm install -g pnpm@10
          echo "✅ PNPM instalado com sucesso:"
          pnpm -v

      # 2️⃣ Configurar Node.js
      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      # 3️⃣ Instalar dependências
      - name: 📦 Instalar dependências
        run: pnpm install --no-frozen-lockfile

      # 4️⃣ Executar script de relatório
      - name: 🧠 Gerar Relatório Semanal (Notion)
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_SPRINT_DB_ID: ${{ secrets.NOTION_SPRINT_DB_ID }}
        run: npx tsx scripts/weekly-report.ts

      # 5️⃣ Commit automático
      - name: 💾 Commit Report
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add reports/
          git diff --cached --quiet || git commit -m "docs: relatório semanal automático"
          git push
