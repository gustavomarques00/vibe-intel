{
  "pipeline": {
    "build": {
      "outputs": ["dist/**", ".next/**"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "outputs": []
    },
      "test": {
      "outputs": ["coverage/**"],
      "dependsOn": ["build"]
    }
  }
}
